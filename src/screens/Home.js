import React from "react";
import {
  SafeAreaView,
  View,
  StatusBar,
  Text,
  TextInput,
  FlatList,
  Dimensions,
  StyleSheet,
  Image,
  Pressable,
  ScrollView,
  TouchableOpacity,
} from 'react-native';

const Home= () =>{
  return (
    <Text>HomePage</Text>
  )
}
const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems:"center"
  },
})
export default Home;